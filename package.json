{
  "name": "blastwave-landing",
  "version": "1.0.0",
  "description": "Blastwave Landing Page",
  "scripts": {
    "serve": "npx serve . -p 3000",
    "dev": "npx live-server --port=3000",
    "build:css": "tailwindcss -i ./src/input.css -o ./assets/styles.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
